From 88a520792e991025b28a776ed71d7357cf4d3b60 Mon Sep 17 00:00:00 2001
From: csev1755 <49533775+csev1755@users.noreply.github.com>
Date: Sat, 14 Mar 2026 16:49:48 -0500
Subject: [PATCH] ncs601w config

Signed-off-by: csev1755 <49533775+csev1755@users.noreply.github.com>
---
 .config                                       | 32 ++++++++++
 .gitignore                                    |  2 -
 files/etc/config/audio-streamer               |  2 +
 files/etc/config/ir-led                       |  4 ++
 files/etc/init.d/audio-streamer               | 42 +++++++++++++
 files/etc/init.d/ir-led                       | 60 +++++++++++++++++++
 files/etc/modules.d/video-uvc                 |  1 +
 files/etc/uci-defaults/99-custom              |  4 ++
 include/target.mk                             |  2 +-
 .../ramips/dts/rt5350_wansview_ncs601w.dts    |  5 ++
 target/linux/ramips/image/rt305x.mk           |  3 +-
 11 files changed, 153 insertions(+), 4 deletions(-)
 create mode 100644 .config
 create mode 100644 files/etc/config/audio-streamer
 create mode 100644 files/etc/config/ir-led
 create mode 100755 files/etc/init.d/audio-streamer
 create mode 100755 files/etc/init.d/ir-led
 create mode 100644 files/etc/modules.d/video-uvc
 create mode 100755 files/etc/uci-defaults/99-custom

diff --git a/.config b/.config
new file mode 100644
index 0000000000..646c1cc2ee
--- /dev/null
+++ b/.config
@@ -0,0 +1,32 @@
+CONFIG_TARGET_ramips=y
+CONFIG_TARGET_ramips_rt305x=y
+CONFIG_TARGET_ramips_rt305x_DEVICE_wansview_ncs601w=y
+CONFIG_PACKAGE_alsa-lib=y
+CONFIG_PACKAGE_glib2=y
+CONFIG_PACKAGE_gst1-mod-alaw=y
+CONFIG_PACKAGE_gst1-mod-alsa=y
+CONFIG_PACKAGE_gst1-mod-mulaw=y
+CONFIG_PACKAGE_gst1-mod-rtp=y
+CONFIG_PACKAGE_gst1-mod-udp=y
+CONFIG_PACKAGE_gstreamer1-libs=y
+CONFIG_PACKAGE_gstreamer1-plugins-base=y
+CONFIG_PACKAGE_gstreamer1-plugins-good=y
+CONFIG_PACKAGE_gstreamer1-utils=y
+CONFIG_PACKAGE_libatomic=y
+CONFIG_PACKAGE_libattr=y
+CONFIG_PACKAGE_libffi=y
+CONFIG_PACKAGE_libgst1audio=y
+CONFIG_PACKAGE_libgst1net=y
+CONFIG_PACKAGE_libgst1pbutils=y
+CONFIG_PACKAGE_libgst1rtp=y
+CONFIG_PACKAGE_libgst1tag=y
+CONFIG_PACKAGE_libgstreamer1=y
+CONFIG_PACKAGE_libjpeg-turbo=y
+CONFIG_PACKAGE_libpcre2=y
+CONFIG_PACKAGE_libpthread=y
+CONFIG_PACKAGE_librt=y
+CONFIG_PACKAGE_libxml2=y
+CONFIG_PACKAGE_mjpg-streamer=y
+CONFIG_PACKAGE_mjpg-streamer-input-uvc=y
+CONFIG_PACKAGE_mjpg-streamer-output-http=y
+# CONFIG_PACKAGE_libgst1video is not set
diff --git a/.gitignore b/.gitignore
index ad04755919..3dd918dbec 100644
--- a/.gitignore
+++ b/.gitignore
@@ -3,7 +3,6 @@
 .*.swp
 /env
 /dl
-/.config
 /.config.old
 /.toolchain_build_ver
 /bin
@@ -13,7 +12,6 @@
 /logs
 /feeds
 /feeds.conf
-/files
 /target/linux/feeds
 /overlay
 /package/feeds
diff --git a/files/etc/config/audio-streamer b/files/etc/config/audio-streamer
new file mode 100644
index 0000000000..cdcbe42690
--- /dev/null
+++ b/files/etc/config/audio-streamer
@@ -0,0 +1,2 @@
+config audio-streamer
+    option enabled '0'
diff --git a/files/etc/config/ir-led b/files/etc/config/ir-led
new file mode 100644
index 0000000000..839e7f8669
--- /dev/null
+++ b/files/etc/config/ir-led
@@ -0,0 +1,4 @@
+config ir-led
+    option enabled '0'
+    option sensor '519'
+    option led '520'
diff --git a/files/etc/init.d/audio-streamer b/files/etc/init.d/audio-streamer
new file mode 100755
index 0000000000..0a46c1a1ff
--- /dev/null
+++ b/files/etc/init.d/audio-streamer
@@ -0,0 +1,42 @@
+#!/bin/sh /etc/rc.common
+
+START=90
+STOP=10
+
+USE_PROCD=1
+PROG=/usr/bin/gst-launch-1.0
+error() {
+	echo "${initscript}:" "$@" 1>&2
+}
+
+start_instance() {
+    local s="$1"
+
+    local enabled
+    config_get_bool enabled "$s" 'enabled' 0
+    [ "$enabled" -gt 0 ] || return
+
+    local host port
+    config_get host "$s" 'host'
+    config_get port "$s" 'port'
+
+    if [ -z "$host" ] || [ -z "$port" ]; then
+        error "Missing required 'host' or 'port' in config section '$s'"
+        return 1
+    fi
+
+    procd_open_instance
+    procd_set_param command "$PROG" -v alsasrc device=hw:0 ! \
+        "audio/x-raw,format=S16LE,channels=1,rate=8000" ! alawenc ! \
+        rtppcmapay ! udpsink host=${host} port=${port}
+    procd_close_instance
+}
+
+start_service() {
+    config_load 'audio-streamer'
+    config_foreach start_instance 'audio-streamer'
+}
+
+service_triggers() {
+	procd_add_reload_trigger 'audio-streamer'
+}
diff --git a/files/etc/init.d/ir-led b/files/etc/init.d/ir-led
new file mode 100755
index 0000000000..1994e8938c
--- /dev/null
+++ b/files/etc/init.d/ir-led
@@ -0,0 +1,60 @@
+#!/bin/sh /etc/rc.common
+
+START=95
+STOP=10
+
+USE_PROCD=1
+
+start_service() {
+	config_load 'ir-led'
+	config_foreach start_instance 'ir-led'
+}
+
+start_instance() {
+	local section="$1"
+
+	local enabled sensor led
+	config_get_bool enabled "$section" 'enabled' 0
+	config_get sensor "$section" 'sensor'
+	config_get led "$section" 'led'
+
+	[ "$enabled" -gt 0 ] || return
+
+	# Validate GPIOs
+	[ -n "$sensor" ] || { echo "Missing 'sensor' GPIO in config"; return 1; }
+	[ -n "$led" ] || { echo "Missing 'led' GPIO in config"; return 1; }
+
+	# Export GPIOs if not already exported
+	[ -d /sys/class/gpio/gpio${sensor} ] || echo "$sensor" > /sys/class/gpio/export
+	[ -d /sys/class/gpio/gpio${led} ] || echo "$led" > /sys/class/gpio/export
+
+	# Set directions
+	echo "in" > /sys/class/gpio/gpio${sensor}/direction
+	echo "out" > /sys/class/gpio/gpio${led}/direction
+
+	# Start the passthrough loop
+	procd_open_instance
+	procd_set_param command /bin/sh -c "
+		while true; do
+			cat /sys/class/gpio/gpio${sensor}/value > /sys/class/gpio/gpio${led}/value
+			sleep 1
+		done"
+	procd_set_param respawn
+	procd_close_instance
+}
+
+stop_service() {
+	config_load 'ir-led'
+	config_foreach stop_instance 'ir-led'
+}
+
+stop_instance() {
+	local section="$1"
+
+	local sensor led
+	config_get sensor "$section" 'sensor'
+	config_get led "$section" 'led'
+
+	[ -d /sys/class/gpio/gpio${sensor} ] && echo "$sensor" > /sys/class/gpio/unexport
+	[ -d /sys/class/gpio/gpio${led} ] && echo "$led" > /sys/class/gpio/unexport
+}
diff --git a/files/etc/modules.d/video-uvc b/files/etc/modules.d/video-uvc
new file mode 100644
index 0000000000..5b63014842
--- /dev/null
+++ b/files/etc/modules.d/video-uvc
@@ -0,0 +1 @@
+uvcvideo quirks=0x22
diff --git a/files/etc/uci-defaults/99-custom b/files/etc/uci-defaults/99-custom
new file mode 100755
index 0000000000..09839176a9
--- /dev/null
+++ b/files/etc/uci-defaults/99-custom
@@ -0,0 +1,4 @@
+uci -q batch << EOI
+set network.lan.proto='dhcp'
+delete network.lan.ipaddr
+EOI
diff --git a/include/target.mk b/include/target.mk
index 45d0a6686b..a28d886c0d 100644
--- a/include/target.mk
+++ b/include/target.mk
@@ -10,7 +10,7 @@ __target_inc=1
 ##@
 # @brief Default device type ( basic | nas | router ).
 ##
-DEVICE_TYPE?=router
+DEVICE_TYPE?=basic
 
 ##@
 # @brief Default packages.
diff --git a/target/linux/ramips/dts/rt5350_wansview_ncs601w.dts b/target/linux/ramips/dts/rt5350_wansview_ncs601w.dts
index 52be1d7284..d0fb235379 100644
--- a/target/linux/ramips/dts/rt5350_wansview_ncs601w.dts
+++ b/target/linux/ramips/dts/rt5350_wansview_ncs601w.dts
@@ -3,6 +3,11 @@
 / {
 	compatible = "wansview,ncs601w", "ralink,rt5350-soc";
 	model = "Wansview NCS601W";
+
+	memory@0 {
+		reg = <0x0 0x4000000>;
+		device_type = "memory";
+	};
 };
 
 &spi0 {
diff --git a/target/linux/ramips/image/rt305x.mk b/target/linux/ramips/image/rt305x.mk
index f52098c733..1a2ab20c4a 100644
--- a/target/linux/ramips/image/rt305x.mk
+++ b/target/linux/ramips/image/rt305x.mk
@@ -1177,11 +1177,12 @@ endef
 TARGET_DEVICES += vocore_vocore-8m
 
 define Device/wansview_ncs601w
+  $(Device/uimage-lzma-loader)
   SOC := rt5350
   IMAGE_SIZE := 7872k
   DEVICE_VENDOR := Wansview
   DEVICE_MODEL := NCS601W
-  DEVICE_PACKAGES := kmod-video-core kmod-video-uvc kmod-usb-ohci
+  DEVICE_PACKAGES := kmod-video-core kmod-video-uvc kmod-usb-ohci kmod-sound-core kmod-usb-audio
   SUPPORTED_DEVICES += ncs601w
 endef
 TARGET_DEVICES += wansview_ncs601w
-- 
2.53.0

